
export function generateKeyPair() {

    console.log(`generate a new key pair public and private bitcoin keys`)
}

export function buildTransaction(tx: Transaction) {

    console.log(`return transaction in the version ${tx.version}}`)
}
